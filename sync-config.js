// sync-config.js — only one value to set, no tokens here
// After deploying your Cloudflare Worker, paste its URL below.
// e.g. 'https://runtrack-sync.yourname.workers.dev'
const SYNC_WORKER_URL = 'https://YOUR_WORKER.workers.dev';
